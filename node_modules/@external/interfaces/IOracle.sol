// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "./IPrice.sol";

// Another external interface
interface IOracle {
    function getPrice(address token) external view returns (int256);
    function getPriceFeed(address token) external view returns (IPrice);
}
