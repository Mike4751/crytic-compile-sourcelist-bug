[profile.default]
src = "nonexistent"
test = "."
out = "out"
libs = ["../../node_modules", "../../lib"]
allow_paths = ["../.."]
build_info = true
remappings = [
    "@external/=../../node_modules/@external/",
    "mylib/=../../lib/mylib/",
    "contracts/=../../contracts/"
]
